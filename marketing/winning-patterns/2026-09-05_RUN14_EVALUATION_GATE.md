# ProofBerry Growth Run 14 — Analytics / Winner Gate

## Current telemetry

Run 14 queried the live ProofBerry `GrowthEvent` entity and found 20 records:
- 18 `landing_view`
- 2 `waitlist_form_view`
- 0 `audience_select`
- 0 `waitlist_form_start`
- 0 `waitlist_submit`

Observed events are direct / organic / default and may include internal or test traffic. There is still no qualified post-PB-LP-003 external cohort.

## Experiment discipline

- `PB-EXP-008` remains running; landing UX is held stable.
- `PB-EXP-009` remains the consolidated recruiter/employer messaging experiment.
- PB-DOC-001 / PB-TXT-022 are queued outside the active challenger set until they receive verified attributed distribution.
- No new experiment family was opened.

## Winner gate

No asset becomes a Winner Library entry unless all of the following are true:
1. Publication is verified with a platform post ID or URL.
2. Traffic is attributable to the exact asset through UTM/content variant.
3. The audience is relevant to the stated experiment.
4. Comparable variants reach the preregistered sample threshold of at least 100 attributed landing sessions or 20 attributed waitlist signups per comparable variant.
5. Qualified waitlist conversion, not impressions alone, supports the decision.

## Run 14 decision

**No winner declared.**

The highest-value next measurement is whether the concrete recruiter review-packet artifact attracts qualified recruiter/employer form starts and signups once a supported channel is authenticated.
